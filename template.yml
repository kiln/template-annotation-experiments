id: 'annotation'
name: Annotation experiment
author: Mr Annotate
sdk_version: 2

build:
  src:
    script: npm run build
    directory: src
    files:
      - rollup.config.js

  less:
    script: npm run less
    directory: less

settings:

data:
